specVersion: "v1beta"
serviceId: "tabletap"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tabletap-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
